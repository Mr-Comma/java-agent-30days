# Manual Run Report

- Time: 2026-06-09 10:10 CST
- Scope: Check current Java Agent task status and start the local Spring Boot service

## 当前状态

- 项目处于 Day 2/Day 3 交界：已有最小 Spring Boot `/chat` 接口。
- 今日日志 `logs/2026-06-09.md` 已记录 time 工具调用能力。
- 当前 8080 端口已启动 Java 服务并保持监听。

## 本次操作

- 阅读 `PLAN.md`、`DAILY_RULE.md`、今日日志和最新自动运行报告。
- 执行 `mvn test`，确认 3 个测试全部通过。
- 初次 `mvn spring-boot:run` 后发现环境/目录问题，改为在项目目录中使用完整插件坐标启动。
- 服务启动命令：`JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-17.jdk/Contents/Home /usr/local/apache-maven-3.6.3/bin/mvn org.springframework.boot:spring-boot-maven-plugin:3.3.6:run`

## 验证结果

- `mvn test`：通过，3 个测试全部成功。
- `lsof -nP -iTCP:8080 -sTCP:LISTEN`：确认 Java 进程监听 8080。
- `curl "http://localhost:8080/chat?prompt=hello"`：返回 mock agent JSON。
- `curl --get --data-urlencode "prompt=现在几点" "http://localhost:8080/chat"`：返回 `Tool[time]` 当前时间 JSON。

## 注意事项

- 中文 prompt 需要 URL 编码；直接拼中文 query 可能被 Tomcat 判为 400 Bad Request。
- 后续可以补一个 README 示例，使用 `curl --get --data-urlencode` 展示中文请求。

## 下一步建议

- Day 3：把 time 工具从 `if` 判断抽象成 Tool 接口或 ToolRegistry。
