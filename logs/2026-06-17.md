# 2026-06-17 Java Agent 学习日志

## 今日主题
补齐 `/api-docs/analyze` 的 READY 调试链路文档示例。

## 今日完成
- 在 README 增加解析到接口时的 READY 请求示例。
- 补充 `workflowStatus`、`workflowStage`、`suggestedTool`、`debugHints`、`reviewPromptVariables` 和 `reviewPromptPreview` 的 READY 关键响应片段。
- 扩展 README 契约测试，锁定 READY 调试字段，避免文档和确定性响应漂移。
- 追加 READY/NEEDS_INPUT 调试字段说明表，明确每个字段在前端和 Agent 节点里的使用方式。
- 扩展 README 契约测试，锁定字段说明表的关键行，降低后续文档漂移风险。
- 新增 `GET /api-docs/debug-schema` mock 端点，直接返回调试字段的 READY/NEEDS_INPUT 含义和前端/Agent 用法。
- 增加 Controller JSON 断言，锁定字段 schema 的关键顺序和文案，方便后续调试面板接入。

## 验证
- 已执行 `mvn test`，17 个测试全部通过。

## 今日观点
调试面板文档应该同时覆盖 READY 与 NEEDS_INPUT 两条路径，这样前端和 Agent 编排节点能明确区分“可审查”和“需补输入”。

## Next
- 下一轮建议：为 `GET /api-docs/debug-schema` 增加 README 响应片段或轻量 schema 契约文档，让前端接入时能直接对照字段元信息。
