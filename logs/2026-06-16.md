# 2026-06-16 Java Agent 学习日志

## 今日主题
把结构化 Prompt 变量进一步渲染成可执行审查请求预览。

## 今日完成
- 为 `/api-docs/analyze` 响应新增 `reviewPromptPreview` 字段。
- READY 场景会生成“调用 `api-risk-reviewer` 审查首要模块”的预览文本。
- NEEDS_INPUT 场景会生成“调用 `openapi-input-validator` 处理输入阻塞”的预览文本，并保持不编造接口。
- 增加轻量 Controller JSON 契约测试，确认 `reviewPromptVariables` 与 `reviewPromptPreview` 能通过 HTTP 响应暴露。
- 补充 NEEDS_INPUT 的 Controller JSON 契约测试，锁定 `blockingReason`、`suggestedTool`、`reviewPromptVariables` 和 `reviewPromptPreview` 的缺输入响应。
- 更新 README 调试说明，标出前端/Agent 节点可重点读取的工作流字段。
- 补充 README 的 NEEDS_INPUT 最小 curl 请求与关键响应片段，方便前端/Agent 节点调试缺输入链路。
- 增加 README 示例契约测试，确保缺输入调试字段和预览文本不会从文档中漂移。

## 验证
- 已执行 `mvn test`，14 个测试全部通过。

## 今日观点
结构化变量适合给系统消费，预览文本适合给人和调试面板快速确认下一步动作；两者都应该从同一套确定性上下文派生。

## Next
- 下一轮建议：给 README 增加一个最小 NEEDS_INPUT 响应片段，方便前端/Agent 节点对照调试缺输入链路。
