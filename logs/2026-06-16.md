# 2026-06-16 Java Agent 学习日志

## 今日主题
把结构化 Prompt 变量进一步渲染成可执行审查请求预览。

## 今日完成
- 为 `/api-docs/analyze` 响应新增 `reviewPromptPreview` 字段。
- READY 场景会生成“调用 `api-risk-reviewer` 审查首要模块”的预览文本。
- NEEDS_INPUT 场景会生成“调用 `openapi-input-validator` 处理输入阻塞”的预览文本，并保持不编造接口约束。
- 增加轻量 Controller JSON 契约测试，确认 `reviewPromptVariables` 与 `reviewPromptPreview` 能通过 HTTP 响应暴露。
- 更新 README 调试说明，标出前端/Agent 节点可重点读取的工作流字段。

## 验证
- 已执行 `mvn test`，12 个测试全部通过。

## 今日观点
结构化变量适合给系统消费，预览文本适合给人和调试面板快速确认下一步动作；两者都应该从同一套确定性上下文派生。

## Next
- 下一轮建议：补一个 README 响应片段或最小 curl 验证示例，展示 `reviewPromptVariables` 与 `reviewPromptPreview` 如何帮助前端/Agent 节点调试。
