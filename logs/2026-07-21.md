# 2026-07-21 学习日志

## 今日小目标

为 `/api-docs/debug-schema` 补齐一个极小的 JSON 序列化 smoke test，专门锁定顶层路由允许值，避免枚举与 HTTP/JSON 契约发生漂移。

## 完成内容

- 接续 2026-07-20 的未完成任务，新增 `ApiDocDebugSchemaRoutingSmokeTest`。
- 测试通过 Jackson 真实序列化/反序列化 `ApiDocDebugSchemaService.schema()`，检查：
  - `workflowStatusAllowedValues` 与 `ApiWorkflowStatus.allowedValues()` 一致。
  - `workflowStageAllowedValues` 与 `ApiWorkflowStage.allowedValues()` 一致。
  - `suggestedToolAllowedValues` 与 `ApiSuggestedTool.allowedValues()` 一致。
- 修正测试实现，避免使用当前 Jackson 版本不支持的 `JsonNode.valueStream()`。

## 验证

- `JAVA_HOME=$(/usr/libexec/java_home -v 17) /usr/local/apache-maven-3.6.3/bin/mvn -Dtest=ApiDocDebugSchemaRoutingSmokeTest test`：通过，1 tests，0 failures，0 errors，0 skipped。
- 完整 `mvn test` 待本轮收尾前运行。

## 今日观点

面向 Agent 编排层的 debug schema 不只要有大而全的字段清单，也需要几个“针尖级”smoke test 锁住最关键的路由契约；这样后续新增字段时，核心状态、阶段和工具名不会悄悄漂移。

## 下一步

- 为 `/api-docs/analyze` 设计一个最小“工具调用计划”字段，例如只先输出 tool、reason、payloadKeys，连接后续真实 Agent 节点。
