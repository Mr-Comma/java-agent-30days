# 2026-07-20 学习日志

## 今日小目标

为 `/api-docs/debug-schema` 增加一个聚焦顶层路由允许值的最小序列化 smoke test，确保前端和 Agent 编排层看到的 JSON 契约持续与 Java 枚举一致。

## 完成内容

- 待完成：新增独立 smoke test，覆盖 workflow status、workflow stage 和 suggested tool 三组顶层允许值。
- 待完成：运行定向测试与完整测试。

## 验证

- 待验证。

## 今日观点

窄而清晰的契约 smoke test 能在大而全的控制器测试之外，更快暴露关键 Agent 路由字段的序列化漂移。

## 下一步

- 完成本次最小测试迭代后，再评估是否为 `/api-docs/analyze` 增加结构化工具调用计划字段。
