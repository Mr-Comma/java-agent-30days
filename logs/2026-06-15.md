# 2026-06-15 Java Agent 学习日志

## 今日主题
补齐 6 月 13-15 日断档记录，并完成一个小代码迭代。

## 今日完成
- 为 `/api-docs/analyze` 响应新增 `workflowStage` 字段。
- `READY` 映射为 `REVIEW_READY`；`NEEDS_INPUT` 映射为 `INPUT_REQUIRED`。
- 更新单元测试、README 和运行报告。

## 验证
- 已执行 Maven 测试，确保 mock-first API 文档助手行为稳定。

## 今日观点
补进度时不要只补日志，也要尽量补一个可运行、可测试、可提交的小增量，这样 GitHub 记录才有真实价值。

## Iteration 09:56

### Done

- 复核并确认 `/api-docs/analyze` 已新增 `workflowStage` 字段：`READY -> REVIEW_READY`，`NEEDS_INPUT -> INPUT_REQUIRED`。
- 用 Java 17 运行 `mvn test`，确认 11 个测试全部通过。
- 补充本轮运行报告，记录 Java 8 Maven 默认运行时导致的初次验证失败和 Java 17 复测结果。

### Learned

- 工作流阶段字段适合从已有 `workflowStatus` 派生，避免为了 UI/编排便利引入第二套状态机。
- 本机 Maven 默认 Java 8，Spring Boot 3 / Java 17 项目验证时应显式设置 `JAVA_HOME`。

### Next

- 下一轮建议：增加 `suggestedTool` 或 `reviewPromptTemplate`，把阶段、阻塞原因和首个审查动作组合成后续 Agent 节点可直接消费的执行上下文。

## Iteration 10:00

### Done

- 为 `/api-docs/analyze` 响应新增 `suggestedTool` 字段。
- `READY` 场景建议 `api-risk-reviewer`，`NEEDS_INPUT` 场景建议 `openapi-input-validator`。
- 更新单元测试和 README，保持 mock-first API 文档助手输出可预测。

### Learned

- 建议工具名应从已有 `workflowStatus` 派生，先给工作流/前端一个稳定路由信号，避免提前引入真实工具编排。

### Next

- 下一轮建议：增加 `reviewPromptTemplate`，把 `workflowStage`、`suggestedTool`、`blockingReason` 和首个审查动作组合成可直接交给 LLM 节点的 prompt 草稿。

## Iteration 14:03

### Done

- 为 `/api-docs/analyze` 响应新增 `reviewPromptTemplate` 字段。
- Prompt 模板复用 `workflowStage`、`suggestedTool`、`blockingReason` 和首个审查动作，保持后续 LLM 节点输入可预测。
- 覆盖 READY 与 NEEDS_INPUT 两条路径，并修正模板拼接中的重复句号问题。

### Learned

- Prompt 草稿也要像 API 字段一样做确定性测试，尤其要检查中文句号和分号拼接，避免把 UI/LLM 可读性问题固化进接口。

### Next

- 下一轮建议：增加一个 `reviewPromptVariables` 结构化字段，把 prompt 模板里的阶段、工具、阻塞原因和首个动作拆成可独立渲染的变量。
