# 2026-07-15 学习日志

## 今日小目标

给 `/api-docs/debug-schema` 补一个 `nextActionCodesByStage` 映射，让 Agent 编排层不仅能校验 `nextActionCode` 是否全局合法，还能校验它是否适用于当前 `stage`。

## 完成内容

- `ApiAnalysisNextActionCode` 新增 `allowedValuesByStage()`，按 `parse`、`aggregate`、`prioritize`、`route`、`advise` 输出稳定映射。
- `ApiDocDebugSchemaResponse` 新增 `nextActionCodesByStage` 字段。
- `ApiDocDebugSchemaService` 直接复用枚举映射生成 schema，避免 README、测试与生产代码分叉。
- Controller/schema 契约测试覆盖阶段映射输出。
- README 和 README 示例测试同步说明阶段级校验用法。

## 验证

- `JAVA_HOME=$(/usr/libexec/java_home -v 17) /usr/local/apache-maven-3.6.3/bin/mvn -Dtest=ApiDocDebugSchemaServiceTest,ApiDocControllerTest test`：通过，5 个测试 0 失败。
- `JAVA_HOME=$(/usr/libexec/java_home -v 17) /usr/local/apache-maven-3.6.3/bin/mvn test`：通过，20 个测试 0 失败。
- `python3 scripts/autonomous_tick.py`：通过，关键文件齐全。

## 下一步

可以继续把 `workflowStatus`、`workflowStage` 和 `suggestedTool` 提取成轻量枚举/常量，减少路由契约字符串漂移。
