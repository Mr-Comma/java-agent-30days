# 2026-06-30 Java Agent 学习日志

## 今日主题
Day 21：继续把 API 文档助手的 debug schema 从“节点策略说明”推进到“可执行节点重试策略”。

## 本轮实现
- 为 `GET /api-docs/debug-schema` 的字段元数据新增 `retryable`。
- `retryable` 标识字段缺失/非法后是否适合自动重试目标节点：可自动降级的工具、调试提示和 Prompt 变量为 `true`，需要补输入或重新分析的状态、阶段、阻塞原因和 Prompt 预览为 `false`。
- 同步更新服务层精确断言、Controller JSON 断言和 README 契约测试，确保运行时 schema、文档示例和字段表一致。

## 自动追加：10:00 迭代
- 为 `GET /api-docs/debug-schema` 的字段元数据继续新增 `operatorMessage`。
- `operatorMessage` 给前端调试面板/Agent 编排层提供中文操作提示：不可重试字段提示补输入或重新分析，可降级字段提示已使用 fallback 并可继续。
- 保持 metadata-only，不改变 `/api-docs/analyze` 的业务行为。

## 验证
- Baseline：`mvn test` 通过，20 tests, 0 failures, 0 errors。
- RED：先新增 `retryable` 断言后，目标测试因缺少 `ApiDocDebugField::retryable` accessor 与构造器参数失败，符合 TDD 预期。
- Targeted：`mvn -q -Dtest=ApiDocDebugSchemaServiceTest,ApiDocControllerTest,ReadmeApiDocExampleTest test` 通过。
- Full：`mvn test` 通过，20 tests, 0 failures, 0 errors。
- 10:00 RED：先新增 `operatorMessage` 服务断言后，目标测试因缺少 `ApiDocDebugField::operatorMessage` accessor 失败，符合 TDD 预期。
- 10:00 Targeted：`mvn -q -Dtest=ApiDocDebugSchemaServiceTest,ApiDocControllerTest,ReadmeApiDocExampleTest test` 通过。

## 今日观点
`policySeverity` 说明策略强度，`retryable` 进一步告诉编排层能不能自动再试一次。两者结合后，Agent 节点可以区分“应用 fallback 后可重试”和“必须补输入/重新分析”，避免无意义循环。追加的 `operatorMessage` 则把机器策略翻译成可展示的人类操作提示，让调试面板能直接告诉用户下一步该补输入、重新分析，还是继续使用降级结果。

## 下一步
- 可以继续为 debug schema 增加 `failureEscalation`，说明不可重试且无法自动降级时应升级给哪个上游/人工角色。
